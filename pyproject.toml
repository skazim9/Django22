[tool.poetry]
name = "django22"
version = "0.1.0"
description = ""
authors = ["MaksimVolkov <skazimko@yandex.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
django = "^5.1.2"
psycopg2-binary = "^2.9.10"
pillow = "^11.0.0"
ipython = "^8.29.0"
redis = "^5.2.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
